<?xml version="1.0" encoding="utf-8"?>
<!-- PIP placeholder background: subtle gradient-like border using solid + stroke -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp" />
    <solid android:color="@color/tv_background" />
    <stroke
        android:width="2dp"
        android:color="@color/tv_primary_brand" />
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
</shape>
